<UserControl
    x:Class="PlexLyricSync.LyricsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel
            Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0 -45 0 0" Spacing="4">
            <Button x:Name="LyUpdate" Visibility="Collapsed" Background="Transparent" BorderBrush="Transparent" Padding="0" Height="18" Width="18">
                <FontIcon x:Name="RefreshIcon" Glyph="&#xe777;" FontFamily="Segoe Fluent Icons" Foreground="White" FontSize="10"/>
            </Button>
            <TextBlock x:Name="LySource" VerticalAlignment="Top" FontSize="12" Foreground="White" Opacity="0.7"/>
        </StackPanel>

        <ScrollViewer x:Name="LyScroll" Grid.Row="0" Margin="0 -61 0 0" VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Hidden">
            <StackPanel x:Name="LyStack" VerticalAlignment="Top" Spacing="2"/>
        </ScrollViewer>
    </Grid>
</UserControl>
